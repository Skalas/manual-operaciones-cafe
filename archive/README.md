# Archivo histórico

Artefactos de la migración inicial del manual (de un único `.md` al sitio MkDocs).
Se conservan solo como referencia; **no forman parte del build** y no deben editarse.

| Archivo | Qué fue |
| ----- | ----- |
| `Manual de Operación.md` | Documento original monolítico, antes de dividirse en `docs/`. |
| `fix_heading_levels.py` | Script puntual para normalizar niveles de encabezado durante la migración. |
| `fix_list_indent.py` | Script puntual para corregir la indentación de listas durante la migración. |

El git conserva el historial completo; esta carpeta solo evita ruido en la raíz y en `scripts/`.
